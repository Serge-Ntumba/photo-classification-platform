"""Shared pytest fixtures are defined at repository root.

Keeping the implementation in ``/conftest.py`` makes the same fixtures available
to both ``pytest services/main`` and ``pytest tests/contracts``.
"""
