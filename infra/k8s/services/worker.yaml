apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: photo-classification
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: worker-metrics
      port: 9100
      targetPort: 9100
