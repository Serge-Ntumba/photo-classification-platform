apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: photo-classification
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 8000
      targetPort: http
