apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: photo-classification
spec:
  type: ClusterIP
  selector:
    app: minio
  ports:
    - name: s3
      port: 9000
      targetPort: s3
